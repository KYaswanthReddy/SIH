# SIH26012 Full Architecture (PMG + DG + Connectivity) Quick Multi-Epoch Training Configuration
# Fast verification of multi-epoch learning capability on 500 train, 100 val, 100 test patches @ 256x256.

experiment:
  name: "quick_full_cadastre_network"
  output_dir: "experiments/quick_full"
  checkpoint_dir: "experiments/quick_full/checkpoints"
  metrics_dir: "experiments/quick_full/metrics"
  predictions_dir: "experiments/quick_full/predictions"
  seed: 42

data:
  processed_dir: "data/processed"
  metadata_dir: "data/processed/metadata"
  train_metadata: "data/processed/metadata/patches_train.csv"
  val_metadata: "data/processed/metadata/patches_val.csv"
  test_metadata: "data/processed/metadata/patches_test.csv"
  max_train_samples: 500
  max_val_samples: 100
  max_test_samples: 100
  image_size: [256, 256]
  in_channels: 3
  out_channels: 1

model:
  architecture: "CadastreUNetFull"
  encoder_channels: [32, 64, 128, 256, 512]
  pmg_channels: 128
  dropout: 0.1
  pmg:
    enabled: true
    scales: 4
  domain_generalization:
    enabled: true
    prob: 0.5
  connectivity:
    enabled: true
    num_directions: 8

loss:
  boundary_loss_weight: 1.0
  connectivity_loss_weight: 0.5
  focal_weight: 0.5
  dice_weight: 0.5
  focal:
    alpha: 0.25
    gamma: 2.0
  dice:
    smooth: 1.0

training:
  epochs: 5
  batch_size: 8
  learning_rate: 1.0e-4
  weight_decay: 1.0e-4
  optimizer: "AdamW"
  scheduler: "CosineAnnealingLR"
  early_stopping_patience: 5
  mixed_precision: true
  num_workers: 2
  pin_memory: true

evaluation:
  thresholds: [0.3, 0.4, 0.5, 0.6, 0.7]
  default_threshold: 0.5
